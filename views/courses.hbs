<h1>Courses for mentally gifted</h1>

{{#if courses.length}}

{{#each courses}}

<div class="row">
	<div class="col s6 offset-s3">
		<div class="card">
			<div class="card-image">
				<img src={{image}}>
			</div>
			<div class="card-content">
				<span class="card-title">{{title}}</span>
				<p class="price">{{price}}</p>
			</div>

			<div class="card-action actions">
				<a href="/courses/{{id}}" target="_blank">Open course</a>

				<form action="/card/add" method="POST">
					<input hidden type="text" name="id" value="{{id}}">

					<button type="submit" class="btn btn-primary">Buy</button>
				</form>
			</div>

			<div class="card-action actions">

				<a href="/courses/{{id}}/editcourse?allow=true">Edit course</a>
				<a href="/courses/{{id}}/deletecourse?allow=true">Delete course</a>
			</div>

		</div>
	</div>
</div>

{{/each}}

{{else}}

<p>Курсов пока нет, подождите пока кто нибудь снизойдет до их создания</p>

{{/if}}